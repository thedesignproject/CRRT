# CRRT extension icons

The Store package includes exact-size 16, 32, 48, and 128 pixel derivatives
of `branding/design-system-crrt/Frame 11.png`, the canonical CRRT carrot mark.
Keeping the source outside `public/` prevents release documentation from being
copied into the browser extension bundle.
